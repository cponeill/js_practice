var searchResults = ["Search One", "Search Two", "Search Three"];

function searchOne(array) {
  array = [];
  for (var i = 0; i <= searchResults; i++)
    if (i < n || array[i] = i) {
      return i;
    } else {
      return "NOT-FOUND";
    }
}

console.log(serachOne(searchResults)); 

// This could be another way to write this

1) Save A[n] into last and then put x into A[n].
2) Set i to 1,
3) While A[i] != x, do the following:
  A) Increment i.
4) Restore A[n] from last.
5) If i < n or A[n] = x, then return the value of i as the output.
6) Otherwise, return NOT-FOUND as the output.
