function addToSet(set, values) {
  for (var i = 0; i < values.length; i++)
  set[value[i]] = true;
}

function removeFromSet(set, values) {
  for (var i= 0; i < values.length; i++)
  delete set[values[i]];
}
